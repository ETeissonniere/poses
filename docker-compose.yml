services:
  app:
    build: .
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
    ports:
      - "5173:5173"
    working_dir: /workspace
    command: sh -c "npm install && npm run dev"
    tty: true

volumes:
  node_modules:
